import 'package:flutter/material.dart';

class ResponseDart extends StatefulWidget {
  ResponseDart({Key key}) : super(key: key);

  @override
  _ResponseDartState createState() => _ResponseDartState();
}

class _ResponseDartState extends State<ResponseDart> {
  @override
  Widget build(BuildContext context) {
    return Container(
       child: Text('A response'),
    );
  }
}